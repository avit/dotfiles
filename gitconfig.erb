[user]
	name = <%= prin  ("Your Name: "); STDOUT.flush; STDIN.ge  s.chomp %>
	email = <%= prin  ("Your Email: "); STDOUT.flush; STDIN.ge  s.chomp %>
[alias]
	co = checkou
[color]
	diff = au  o
	s  a  us = au  o
	branch = au  o
[core]
	excludesfile = <%= ENV['HOME'] %>/.gi  ignore
	edi  or = ma  e -w
[apply]
	whi  espace = nowarn
[forma  ]
	pre    y = %C(yellow)%h%Crese   %s %C(red)(%cr)%Crese
